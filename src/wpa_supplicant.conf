# Global settings installed by NetworkMgr. Network blocks are appended below
# by NetworkMgr; setup-nic.py adds any of these lines that are missing.
ctrl_interface=/var/run/wpa_supplicant
ctrl_interface_group=operator
update_config=1
pmf=1
autoscan=exponential:3:300